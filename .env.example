# 默认保存连接数据的路径，如不设置则使用用户目录下的.mcp-ssh文件夹
DATA_PATH=

# 自动保存连接历史记录
SAVE_HISTORY=true

# 默认SSH端口
DEFAULT_SSH_PORT=22

# 连接超时时间（毫秒）
CONNECTION_TIMEOUT=10000

# 自动重连尝试次数
RECONNECT_ATTEMPTS=3

# 记住密码的过期时间（天），0表示永不过期
PASSWORD_EXPIRY_DAYS=30

# 默认命令执行超时时间（毫秒），0表示无限
COMMAND_TIMEOUT=60000 